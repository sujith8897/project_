import './style.css';

export default function Navbar() {
    return(
        <div class="navbar">
            <p class="logo">Logo</p>
            <button class="btn">CONNECT WALLET</button>
        </div>
    );
}